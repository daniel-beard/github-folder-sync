let orgConfigs = [
    { githubAPIEndpoint = None Text
    , githubAPIToken = Some "$GITHUB_FOLDER_SYNC_TOKEN"
    , orgName = "$GITHUB_FOLDER_SYNC_ORG_NAME" 
    , ignoringRepos = ["nothing"]
    } 
]

in { orgConfigs }